[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "embodied-ai-architect"
version = "1.17.0"
description = "Hardware/Software Codesign Space Exploration and Optimization Environment for Embodied AI systems"
readme = "PYPI_README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Branes-ai Team"}
]
dependencies = [
    "numpy>=1.24.0",
    "pydantic>=2.0.0",
    "matplotlib>=3.7.0",
    "jinja2>=3.1.0",
    "click>=8.1.0",
    "rich>=13.0.0",
    "pyyaml>=6.0",
    "langgraph>=0.2.0",
]

[project.optional-dependencies]
models = [
    "torch>=2.0.0",
]
zoo = [
    "ultralytics>=8.0.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "build>=1.0.0",
]
remote = [
    "paramiko>=3.0.0",  # SSH connections
]
kubernetes = [
    "kubernetes>=28.0.0",  # K8s client
]
chat = [
    "anthropic>=0.39.0",  # Claude API for interactive chat
]
optimization = [
    "pymoo>=0.6.0",
    "botorch>=0.11.0",
    "gpytorch>=1.12",
    "scipy>=1.10.0",
]
mcp = [
    "mcp>=1.0.0",
    "uvicorn>=0.25.0",
    "httpx>=0.25.0",
]
api = [
    "fastapi>=0.100.0",
    "uvicorn>=0.25.0",
]
langgraph = [
    "langgraph>=0.2.0",                      # State machine framework
    "langchain-anthropic>=0.3.0",            # Claude integration for LangGraph
    "langgraph-checkpoint-sqlite>=2.0.0",    # SQLite checkpointing for save/resume
]
jetson = [
    "tensorrt>=8.6.0",              # TensorRT for Jetson deployment
    "pycuda>=2022.1",               # CUDA Python bindings for calibration
    "onnxruntime>=1.16.0",          # ONNX Runtime for validation baseline
    "Pillow>=9.0.0",                # Image loading for calibration
]
openvino = [
    "openvino>=2024.0.0",           # OpenVINO for Intel/AMD deployment
    "nncf>=2.7.0",                  # Neural Network Compression Framework for INT8
    "onnxruntime>=1.16.0",          # ONNX Runtime for validation baseline
    "Pillow>=9.0.0",                # Image loading for calibration
]
coral = [
    "tensorflow>=2.13.0",           # TensorFlow for TFLite conversion
    "onnx2tf>=1.20.0",              # ONNX to TensorFlow conversion
    "onnxruntime>=1.16.0",          # ONNX Runtime for validation baseline
    "Pillow>=9.0.0",                # Image loading for calibration
    # Note: pycoral and edgetpu_compiler installed separately on device
]
all = [
    "paramiko>=3.0.0",
    "kubernetes>=28.0.0",
    "anthropic>=0.39.0",
    "langgraph>=0.2.0",
    "langchain-anthropic>=0.3.0",
    "onnxruntime>=1.16.0",
    "Pillow>=9.0.0",
]

[project.scripts]
branes = "embodied_ai_architect.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
embodied_ai_architect = [
    "data/platforms/**/*.yaml",
    "data/sensors/**/*.yaml",
    "data/actuators/**/*.yaml",
]

[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
allow_zero_version = true
major_on_zero = false
branch = "main"
build_command = "python -m build"
commit_message = "chore(release): {version} [skip ci]"
tag_format = "v{version}"

[tool.semantic_release.changelog]
changelog_file = ""

[tool.semantic_release.commit_parser_options]
allowed_tags = ["feat", "fix", "docs", "chore", "refactor", "test", "ci", "perf", "style"]
minor_tags = ["feat"]
patch_tags = ["fix", "perf", "refactor"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "cli: CLI command tests (CliRunner-based)",
    "integration: Multi-component integration tests",
    "slow: Tests that take >5 seconds",
    "requires_api: Requires ANTHROPIC_API_KEY",
]
