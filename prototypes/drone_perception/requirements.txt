# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0

# Detection
ultralytics>=8.0.0

# Tracking & filtering
scipy>=1.11.0
filterpy>=1.4.5

# Visualization
matplotlib>=3.7.0

# Data storage
h5py>=3.9.0

# Configuration and scripting
pyyaml>=6.0

# Optional: Interactive visualization
plotly>=5.17.0

# Optional: Video downloading
# yt-dlp>=2023.0.0

# Phase 2: Stereo cameras (RealSense D435, OAK-D)
pyrealsense2>=2.54.0
depthai>=2.23.0

# Optional: Monocular depth estimation (for generating test depth maps)
# Uncomment these to generate depth maps using MiDaS
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
tqdm>=4.65.0  # Progress bars for depth map generation

# Phase 3: LiDAR + Camera fusion
# Optional: LiDAR support (uncomment based on your hardware)
# python-pcl>=0.3.0  # Point cloud processing (requires PCL installation)
# open3d>=0.18.0  # Alternative point cloud library (easier to install)
# ouster-sdk>=0.10.0  # Ouster LiDAR
# livox-sdk-python>=0.0.2  # Livox LiDAR
# rospy>=1.15.0  # ROS Python (for ROS-based LiDAR)
